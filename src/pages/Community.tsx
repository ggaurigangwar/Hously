import React from 'react';
import { Preview } from './Preview';

export function Community() {
  // We can just reuse the Preview for now
  return (
    <div className="w-full">
      <Preview />
    </div>
  );
}
